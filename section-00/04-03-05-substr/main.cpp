#include <bits/stdc++.h>

using namespace std;

int main() {
  ios::sync_with_stdio(false);

  cin.tie(NULL);
  cout.tie(NULL);

  string full = "aaabbb";

  string sub = full.substr(2);

  cout << sub << endl;

  return 0;
}