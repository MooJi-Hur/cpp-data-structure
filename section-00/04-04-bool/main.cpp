#include <bits/stdc++.h>

using namespace std;

int main() {
  ios_base::sync_with_stdio(false);
  cin.tie(NULL);
  cout.tie(NULL);

  int myNumber = -1;

  cout << bool(myNumber) << endl;

  myNumber = 0;

  cout << bool(myNumber) << endl;

  myNumber = 3;

  cout << bool(myNumber) << endl;

  return 0;
}